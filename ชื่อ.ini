import tkinter as tk

def show_info():
    result_label.config(text="สุวภัทร ปืงเมือง :\nม.5/8 :\n40 :")

root = tk.Tk()
root.title("แสดงข้อมูลนักเรียน")
root.geometry("300x200")

show_button = tk.Button(root, text="แสดงข้อมูล", command=show_info)
show_button.pack(pady=20)

result_label = tk.Label(root, text="", font=("Arial", 12))
result_label.pack()

root.mainloop()
